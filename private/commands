inside geth console:
- loadScript("C:/Users/Thijs/OneDrive/b9lab blockchain study/tictactoe/tictactoe.js");
to load script from browser solidity.

set default account in geth console.
- web3.eth.defaultAccount = eth.accounts[0]
- web3.eth.defaultAccount = eth.accounts[1]

start testrpc
- sudo docker run testrpc

get ip from container
- sudo docker ps
- sudo docker inspect b8f7

compile with solc
- solc -o ./compiled --bin --abi TicTacToe.sol
bin contains the bytecode and abi the json interface

use node compile_deploy.js script
- var tictactoe = require("./compile_deploy.js");
start node inside the build directory

